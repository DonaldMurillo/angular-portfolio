import { CardInCollectionDirective } from './card-in-collection.directive';

describe('CardInCollectionDirective', () => {
  it('should create an instance', () => {
    const directive = new CardInCollectionDirective();
    expect(directive).toBeTruthy();
  });
});
